%script{:src => "/assets/admin/lib/highchart-1.js", :type => "text/javascript"}
#button-statistic
  = link_to admin_root_path(type: "day"), method: :get, class: "btn-statistic day", remote: true do
    = button_tag "Day", class: "btn btn-primary"
  = link_to admin_root_path(type: "week"), method: :get, class: "btn-statistic week", remote: true do
    = button_tag "Week", class: "btn btn-primary"
  = link_to admin_root_path(type: "month"), method: :get, class: "btn-statistic month", remote: true do
    = button_tag "Month", class: "btn btn-primary"
#container{"data-date" => "#{data_user[0]["date"]}", "data-max" => "#{data_user[0]["max"]}", "data-order" => "#{data_order[0]["order"]}", "data-product" => "#{data_product[0]["product"]}", "data-title" => "#{data_user[0]["title"]}", "data-user" => "#{data_user[0]["user"]}", :style => "width: 100%; height: 600px; margin: 0 auto"}
