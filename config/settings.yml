active_record:
  details_info:
    name:
      minimum: 1
      maximum: 50
    number_phone:
      minimum: 6
      maximum: 15
    email:
      regex: /(\A([a-z]*\s*)*\<*([^@\s]+)@((?:[-a-z0-9]+\.)+[a-z]{2,})\>*\Z)/i
  info_injection_book:
    name:
      minimum: 0
      maximum: 50
    number_phone:
      minimum: 6
      maximum: 15
    email:
      regex: /(\A([a-z]*\s*)*\<*([^@\s]+)@((?:[-a-z0-9]+\.)+[a-z]{2,})\>*\Z)/i
  injection_book:
    name:
      minimum: 0
      maximum: 50
  account:
    role:
      admin: 1
      super_admin: 2
      user: 3
      staff: 4
